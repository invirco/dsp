# DSP4 product configuration (boot-time, decision D3)

One firmware image serves D24 and D32. At boot the Pi/CM4 host (D1)
configures each DSP over the normal SPI parameter protocol using the
register block below, then commits. Until CONFIG_COMMIT arrives, the main
loop waits (`_boot_config_received`) and no audio processing runs.

Implementation: `SHARC/src/product_config.asm` (hand infra) +
`SHARC/src/chip*/scope_gates.asm` and the RX patch machinery in
`SHARC/src/chip1/block_io.asm` (both generated by `tools/dsp/dsp_codegen.py`
from `scope=` params in dsp.csv).

## Register block (flat SPI address space, both chips)

| Addr | Name | Semantics |
|---|---|---|
| 0xF000 | PRODUCT_ID | 0 = D32 (default), 1 = D24 |
| 0xF001 | CHAN_MASK | active input strips, bit n = strip n+1 |
| 0xF002 | AUX_MASK | active aux buses |
| 0xF003 | OUT_MUX | bit 0: B_O2 content — 0 = D24 codec, 1 = D32 snake. Stored only; the snake output patch is not generated yet and the TX gather must consume this when it lands |
| 0xF004 | CONFIG_COMMIT | write 1: apply input patch + scope gates, release boot |
| 0xF010+ | INPUT_PATCH[i] | chip 1 only; one register per packed RX channel `i` (see below) |

Addresses ≥ 0xF000 are routed to `_product_config_write` before the
parameter dispatch; parameter writes are bounds-checked against the
generated `_spi_dispatch_cN_size` (fixes the former hardcoded 3904/1820
bounds that rejected newer parameters).

Scope gates: nodes with `scope=D24`/`scope=D32` params and a runtime
enable (today: the AUX_INPUT snake returns) are force-disabled at commit
when PRODUCT_ID differs. Product ids in `scope_gates.asm` and
`product_config.asm` must match (0=D32, 1=D24).

## Boot sequence (per chip, from the Pi)

1. Load DSP boot image over SPI slave boot (BMODE=SPI2 slave).
2. Write PRODUCT_ID, CHAN_MASK, AUX_MASK, OUT_MUX; on chip 1 write the
   INPUT_PATCH table if not identity.
3. Write CONFIG_COMMIT = 1.
4. Stream normal parameter state (scenes, levels) — the DSP is running.

## INPUT_PATCH semantics

`INPUT_PATCH[i] = j` means packed RX DMA channel `i` is delivered to the
slot variable that channel `j` feeds by default (identity when `i == j`).
Packed RX channel order is the block_io sort: (sport, slot) — IN_01..IN_32
are indices 0-31, then codec return (32-34), Pi PCM (35-36), MEMS (37),
snake (38-45). Out-of-range values clamp.

### D24 preset

D24's analog board (D24 Analog rev B netlist, mx26 `docs/d24-analog-xlr-map.md`)
puts eight mic XLRs on each of three AK5558s: U15 = AD0 (J15–J22), U39 = AD1
(J25–J32), U60 = AD2 (J35–J42). AD3 has no D24 ADC (NET-only). Within each
converter the XLRs were placed for layout, not channel number: XLR position
1..8 → AIN 8,7,6,5,3,4,1,2 → **TDM slot 7,6,5,4,2,3,0,1**, and the panel
numbers alternate rows along the board (J15 = ch 1, J16 = ch 13, J17 = ch 2 …).
The patch delivers each converter slot to its panel strip (strip k is default
index k−1; packed RX `i` = 8 × AD + slot):

| DMA ch `i` (AD, slot 0–7) | XLRs on slots 0–7 | Console ch (strip) | INPUT_PATCH[i] |
|---|---|---|---|
| 0-7 (AD0 = U15) | J21 J22 J19 J20 J18 J17 J16 J15 | 4 16 3 15 14 2 13 1 | 3,15,2,14,13,1,12,0 |
| 8-15 (AD1 = U39) | J31 J32 J29 J30 J28 J27 J26 J25 | 8 20 7 19 18 6 17 5 | 7,19,6,18,17,5,16,4 |
| 16-23 (AD2 = U60) | J41 J42 J39 J40 J38 J37 J36 J35 | 12 24 11 23 22 10 21 9 | 11,23,10,22,21,9,20,8 |
| 24-31 (AD3) | — | NET returns 25-32 | 24..31 (identity) |
| 32-45 | — | superset sources | identity |

The per-XLR rows (panel name, 595 chain index and send position, converter,
AIN, slot, packed RX, strip) are `tools/pi/d24_inputs.py`, which computes the
strip from `dsp4_config.D24_INPUT_PATCH` and asserts every XLR lands on its
panel strip. **GENERATED, not typed, since S59**: the table lives at
`defs/products/d24/inputs.csv` (`CONTRACT-PROPOSAL-S58.md`, landed
`defs-v2026.09.16.5`), `tools/dsp/gen_input_patch.py` turns it into
`MW/D24/DSP/input_patch.json`, and `D24_INPUT_PATCH` is loaded from that file
at import (the hand-typed literal is retired).

D24 also boots CHAN_MASK = 0x00FFFFFF (strips 25-32 unused unless NET
sources are patched in).

**Slot order verified on the part (S58, 2026-09-16; was "verify before
bring-up").** The previous table assumed block order (AD slots 0–3 = ch 1–4,
4–7 = ch 13–16) and put MIC 5 on strip 20 (S52-1). With the loop cable on J25
(U39 AIN8 = AD1 slot 7 = packed RX 15) and 1 kHz −20 dBFS on AUX 1: the
pre-S58 patch reads the tone on strip 20, this patch on **strip 5**
(−14.45 dBFS peak raw RX, TEST_MEAS −17.43 dBFS, next strip −102.6), the
identity patch on strip 16 (= `C1_IN_16`, sport 1 slot 7). The part's
resolved `_c1_rx_node_entry[1..24]` equals this table's inverse, and every
one of the 16 XLRs S55 detected on the pre-S58 patch (U39 + U60) now resolves
to its panel strip (findings S58-1/S58-2). U15's rows follow the same
permutation; that section is unpowered and unmeasured.

## Open items

- D32 snake output patch + OUT_MUX consumption in the chip-2 TX gather
  (B_O2 slots are scope-shared with the D24 codec outs).
- Meter/level readback uses the same READ flag path; config registers are
  write-only today.
- This table migrates to the mx_master.csv SOT with the slot map when
  that lands.

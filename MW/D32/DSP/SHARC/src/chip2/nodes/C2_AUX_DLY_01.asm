/*----------------------------------------------------------------------
 * Aux 1 Delay (DELAY)
 * Node ID:    C2_AUX_DLY_01
 * Chip:       2
 * Channels:   1
 * SPI Page:   1
 * SPI Addr:   84
 * RampProfile: InstantCtl
 *
 * AUTO-GENERATED by tools/dsp/dsp_codegen.py — do not edit directly.
 *----------------------------------------------------------------------*/
#include "dsp_block.h"

/* RampProfile: InstantCtl | Mode: Instant | Up: 0ms (0f) Down: 0ms (0f) | Curve: Linear | Scope: Scalar */

/* DELAY: Audio delay line (circular buffer) */
/* SPI page=1 addr=84 */
/* Max: 250.0ms = 12000 samples */

.section/dm seg_delay;
.extern _buf_C2_AUX_LIM_01;
.extern _sample_idx;
.global _dly_buf_C2_AUX_DLY_01;
.var _dly_buf_C2_AUX_DLY_01[12000];  /* L2 SRAM delay buffer */
.section/dm seg_dmda;
.global _dly_write_ptr_C2_AUX_DLY_01;
.var _dly_write_ptr_C2_AUX_DLY_01 = 0;
.global _dly_read_offset_C2_AUX_DLY_01;
.var _dly_read_offset_C2_AUX_DLY_01 = 0;      /* in samples; 0 = no delay */
.global _dly_pool_slot_C2_AUX_DLY_01;
.var _dly_pool_slot_C2_AUX_DLY_01 = -1; /* reserved for future reassignment */
.global _dly_max_C2_AUX_DLY_01;
.var _dly_max_C2_AUX_DLY_01 = 12000;
.global _tap_pre_fader_C2_AUX_DLY_01;
.var _tap_pre_fader_C2_AUX_DLY_01;   /* pre-fader tap */
.global _buf_C2_AUX_DLY_01;
.var _buf_C2_AUX_DLY_01;

        #if DSP4_BLOCK_KERNELS
        .extern _blk_C2_AUX_LIM_01;
        #endif
        #if DSP4_BLOCK_KERNELS
        .global _blk_C2_AUX_DLY_01;
        .var _blk_C2_AUX_DLY_01[DSP4_BLOCK_SIZE];
        .global _bw_k_C2_AUX_DLY_01;
        .var _bw_k_C2_AUX_DLY_01;        /* the caller's _sample_idx */
        .global _bw_s0_C2_AUX_DLY_01;
        .var _bw_s0_C2_AUX_DLY_01;       /* walking source pointer */
        .global _bw_d0_C2_AUX_DLY_01;
        .var _bw_d0_C2_AUX_DLY_01;       /* walking sink pointer */
        #endif

.section/pm seg_pmco;
.global _C2_AUX_DLY_01_process;
_C2_AUX_DLY_01_process:
        #if DSP4_BLOCK_KERNELS
            /* ---- generic per-block wrapper (review finding D16) ----
             * Runs the per-sample reference body BLOCK times over this
             * node's own block buffer, staging each sample through the
             * scalar _buf_ words the body already reads and writes. Same
             * arithmetic, same order, same result as the per-sample
             * build; what it removes is the chain's call/rts and the
             * _sample_idx guard being re-evaluated from the chain.
             *
             * SAMPLE 0 IS PEELED AND _sample_idx IS NOT COUNTED
             * (2026-09-02). The loop used to carry a DM sample index,
             * loading it, storing it to _sample_idx, reloading it,
             * incrementing it and storing it back -- five instructions
             * a sample to drive a guard that only ever asks whether the
             * index is ZERO. Every one of the 119 wrapped bodies was
             * checked: all of them compare it against 0 and none reads
             * it for anything else. So sample 0 runs once outside the
             * loop with _sample_idx = 0, the word is then set to 1, and
             * the remaining BLOCK-1 samples run with the guard shut.
             * The block-rate conversions still fire exactly once per
             * block, which is the property the guard exists for.
             *
             * NOT a free win to generalise: the C2_RECV_* stimulus
             * nodes read _sample_idx & 1 to alternate the profile
             * square, and they would break under this. They are not
             * wrapped nodes -- they carry their own block form -- and
             * that is why the audit is per-node and recorded here.
             * 38 cycles a block per wrapped node.
             */
            i4 = _blk_C2_AUX_LIM_01;
            r3 = i4;
            dm(_bw_s0_C2_AUX_DLY_01) = r3;
            i4 = _blk_C2_AUX_DLY_01;
            r3 = i4;
            dm(_bw_d0_C2_AUX_DLY_01) = r3;
            r5 = dm(_sample_idx);
            dm(_bw_k_C2_AUX_DLY_01) = r5;
            r5 = 0;
            dm(_sample_idx) = r5;
            /* sample 0, peeled: the block-rate guard fires here */
                r3 = dm(_bw_s0_C2_AUX_DLY_01);
                i4 = r3;
                r0 = dm(i4, 0);
                dm(_buf_C2_AUX_LIM_01) = r0;
                r3 = r3 + 1;
                dm(_bw_s0_C2_AUX_DLY_01) = r3;
                call _C2_AUX_DLY_01_process_sample;
                r0 = dm(_buf_C2_AUX_DLY_01);
                r3 = dm(_bw_d0_C2_AUX_DLY_01);
                i4 = r3;
                dm(i4, 0) = r0;
                r3 = r3 + 1;
                dm(_bw_d0_C2_AUX_DLY_01) = r3;
            r5 = 1;
            dm(_sample_idx) = r5;   /* guard shut for 1..BLOCK-1 */
            lcntr = DSP4_BLOCK_SIZE-1, do .bwlp_C2_AUX_DLY_01 until lce;
                r3 = dm(_bw_s0_C2_AUX_DLY_01);
                i4 = r3;
                r0 = dm(i4, 0);
                dm(_buf_C2_AUX_LIM_01) = r0;
                r3 = r3 + 1;
                dm(_bw_s0_C2_AUX_DLY_01) = r3;
                call _C2_AUX_DLY_01_process_sample;
                r0 = dm(_buf_C2_AUX_DLY_01);
                r3 = dm(_bw_d0_C2_AUX_DLY_01);
                i4 = r3;
                dm(i4, 0) = r0;
                r3 = r3 + 1;
            .bwlp_C2_AUX_DLY_01: dm(_bw_d0_C2_AUX_DLY_01) = r3;
            r5 = dm(_bw_k_C2_AUX_DLY_01);
            dm(_sample_idx) = r5;
            rts;

        .global _C2_AUX_DLY_01_process_sample;
        _C2_AUX_DLY_01_process_sample:
        #endif
    r0 = dm(_buf_C2_AUX_LIM_01);

    /* Write to circular buffer at write pointer */
    i0 = _dly_buf_C2_AUX_DLY_01;
    r1 = dm(_dly_write_ptr_C2_AUX_DLY_01);
    m0 = r1;
    modify(i0, m0);
    dm(i0, 0) = r0;

    /* Read from (write_ptr - read_offset) with wrap */
    r2 = dm(_dly_read_offset_C2_AUX_DLY_01);
    r1 = r1 - r2;
    r3 = dm(_dly_max_C2_AUX_DLY_01);
    if lt r1 = r1 + r3;
    i0 = _dly_buf_C2_AUX_DLY_01;
    m0 = r1;
    modify(i0, m0);
    r0 = dm(i0, 0);

    /* Advance write pointer with wrap */
    r1 = dm(_dly_write_ptr_C2_AUX_DLY_01);
    r15 = 1;
    r1 = r1 + r15;
    r3 = dm(_dly_max_C2_AUX_DLY_01);
    comp(r1, r3);
    if ge r1 = r1 - r3;
    dm(_dly_write_ptr_C2_AUX_DLY_01) = r1;

    dm(_tap_pre_fader_C2_AUX_DLY_01) = r0;
    dm(_buf_C2_AUX_DLY_01) = r0;
    rts;
_C2_AUX_DLY_01_process.end:

/* ramp_tables.asm — Ramp profile presets for D32 DSP */
/* AUTO-GENERATED by dsp_codegen.py — do not edit directly. */

/* Frame period: 0.6667 ms (32 samples @ 48 kHz) */

.section/dm seg_dmda;

/* Profile table: { mode, up_frames, down_frames, curve, scope } */
/* mode:  0=Instant, 1=Slew, 2=LinearFrames, 3=ExpFrames */
/* curve: 0=Linear, 1=Exp, 2=Log, 3=S-Curve */
/* scope: 0=Scalar, 1=CoeffSetAtomic */

.global _ramp_profile_table;
_ramp_profile_table:
    /* InstantCtl (ID=0) */
    .var = 0;    /* mode: Instant */
    .var = 0;    /* up: 0ms = 0 frames */
    .var = 0;    /* down: 0ms = 0 frames */
    .var = 0;    /* curve: Linear */
    .var = 0;    /* scope: Scalar */

    /* GainFast (ID=1) */
    .var = 1;    /* mode: Slew */
    .var = 4;    /* up: 3ms = 4 frames */
    .var = 12;    /* down: 8ms = 12 frames */
    .var = 1;    /* curve: Exp */
    .var = 0;    /* scope: Scalar */

    /* GainSafe (ID=2) */
    .var = 1;    /* mode: Slew */
    .var = 15;    /* up: 10ms = 15 frames */
    .var = 45;    /* down: 30ms = 45 frames */
    .var = 1;    /* curve: Exp */
    .var = 0;    /* scope: Scalar */

    /* EqSafe (ID=3) */
    .var = 2;    /* mode: LinearFrames */
    .var = 18;    /* up: 12ms = 18 frames */
    .var = 18;    /* down: 12ms = 18 frames */
    .var = 0;    /* curve: Linear */
    .var = 1;    /* scope: CoeffSetAtomic */

    /* DynSafe (ID=4) */
    .var = 2;    /* mode: LinearFrames */
    .var = 9;    /* up: 6ms = 9 frames */
    .var = 30;    /* down: 20ms = 30 frames */
    .var = 1;    /* curve: Exp */
    .var = 0;    /* scope: Scalar */

/* Individual profile labels (aliases into table above) */
.global _ramp_profile_InstantCtl;
/* _ramp_profile_InstantCtl = _ramp_profile_table + 0 */
.global _ramp_profile_GainFast;
/* _ramp_profile_GainFast = _ramp_profile_table + 5 */
.global _ramp_profile_GainSafe;
/* _ramp_profile_GainSafe = _ramp_profile_table + 10 */
.global _ramp_profile_EqSafe;
/* _ramp_profile_EqSafe = _ramp_profile_table + 15 */
.global _ramp_profile_DynSafe;
/* _ramp_profile_DynSafe = _ramp_profile_table + 20 */

/*----------------------------------------------------------------------
 * Ch 14 Fader (FADER_PAN)
 * Node ID:    C1_FDR_14
 * Chip:       1
 * Channels:   1
 * SPI Page:   1
 * SPI Addr:   1952
 * RampProfile: GainFast
 *
 * AUTO-GENERATED by tools/dsp/dsp_codegen.py — do not edit directly.
 *----------------------------------------------------------------------*/
#include "dsp_block.h"

        /* RampProfile: GainFast | Mode: Slew | Up: 3ms (18f) Down: 8ms (48f) | Curve: Exp | Scope: Scalar */

        #include "blk_pool.h"

        /* FADER_PAN (FIXED Q4.28, D5) — float control, fixed sample path */
        /* SPI page=1 addr=1952 */

        .section/dm seg_dmda;
.extern _buf_C1_DLY_14;
        .global _fdr_level_C1_FDR_14;
        .var _fdr_level_C1_FDR_14 = 1.0;
        .global _fdr_level_target_C1_FDR_14;
        .var _fdr_level_target_C1_FDR_14 = 1.0;
        .global _fdr_level_step_C1_FDR_14;
        .var _fdr_level_step_C1_FDR_14 = 0.0;
        .global _fdr_level_frames_C1_FDR_14;
        .var _fdr_level_frames_C1_FDR_14 = 0;
        .global _fdr_pan_C1_FDR_14;
        .var _fdr_pan_C1_FDR_14 = 0.5;
        .global _fdr_pan_target_C1_FDR_14;
        .var _fdr_pan_target_C1_FDR_14 = 0.5;
        .global _fdr_pan_step_C1_FDR_14;
        .var _fdr_pan_step_C1_FDR_14 = 0.0;
        .global _fdr_pan_frames_C1_FDR_14;
        .var _fdr_pan_frames_C1_FDR_14 = 0;
        .global _fdr_mute_C1_FDR_14;
        .var _fdr_mute_C1_FDR_14 = 0;
        /* DCA IS HOST-MANAGED (PW ruling 2026-08-30, Q2 closed).
         *
         * The CM4 control daemon owns the fold: effective fader =
         * fader dB + DCA dB, mutes OR-ed, written through the level
         * TARGET this node already ramps. Only ramp targets cross the
         * wire, so the DSP needs neither the assignment nor a master
         * gain, and `Dca`/`DcaOn` get no SPI address at all.
         *
         * Both hooks are GONE rather than dormant, which is what the
         * ruling says. `_fdr_dca_sel_` was D57's stored assignment --
         * correct for a cell the kernel must not scale by, and pointless
         * once nothing on the DSP resolves it -- and `_fdr_dca_gain_`
         * was the resolved master gain, sitting at unity and multiplied
         * into the coefficient every block. Removing the multiply is
         * bit-exact: x * 1.0 is exactly x in IEEE 754, and the fader
         * coefficient it feeds is unchanged word for word. */
        .global _fdr_gq_C1_FDR_14;
        .var _fdr_gq_C1_FDR_14 = 0x10000000;          /* Q4.28 level */
        /* 1 while either float ramp still has frames to run. ROUTING's
         * main L/R crosspoint coefficients are this node's pan legs, so it
         * has to keep re-prepping while the pan moves (review finding D22).
         * A ramp is not an SPI write and the control epoch never sees it. */
        #if DSP4_BLOCK_KERNELS
        .global _fdr_busy_C1_FDR_14;
        .global _fdr_busy_C1_FDR_14;
        .var _fdr_busy_C1_FDR_14 = 0;
        #endif

        .global _tap_post_fader_C1_FDR_14;
        .var _tap_post_fader_C1_FDR_14;
        .global _buf_C1_FDR_14;
        .var _buf_C1_FDR_14;
        .global _fdr_lq_C1_FDR_14;
        .var _fdr_lq_C1_FDR_14 = 0;
        .global _fdr_rq_C1_FDR_14;
        .var _fdr_rq_C1_FDR_14 = 0;

        .section/pm seg_pmco;
        .extern _sample_idx;
        .extern _mrf_rns28;
        .global _C1_FDR_14_process;
        _C1_FDR_14_process:
            /* block-rate: float ramps + shadow conversion */
        #if !DSP4_BLOCK_KERNELS
            r4 = dm(_sample_idx);
            r1 = 0;
            comp(r4, r1);
            if ne jump (pc, .apply_C1_FDR_14);
        #endif

        #if DSP4_BLOCK_KERNELS
            /* Ramps-active publication for the control-rate gate (D22).
             * Taken from the frame counts BEFORE they are consumed: the
             * block that runs a ramp's last frames still moves the pan
             * legs, so ROUTING has to prep on that block too.
             *
             * MAX, not OR: unlike the routing sends, these two counters
             * are NOT clamped at zero -- `frames -= BLOCK` on a ramp with
             * fewer than BLOCK frames left leaves a negative count that the
             * snap test then treats as done, and OR-ing a negative word
             * with a live one gives a negative answer. */
            r4 = dm(_fdr_level_frames_C1_FDR_14);
            r1 = dm(_fdr_pan_frames_C1_FDR_14);
            r1 = max(r1, r4);
            r5 = 0;
            r1 = max(r1, r5);
            dm(_fdr_busy_C1_FDR_14) = r1;
        #endif

            /* level ramp */
            r4 = dm(_fdr_level_frames_C1_FDR_14);
            r1 = 0;
            comp(r4, r1);
            if le jump (pc, .lsnap_C1_FDR_14);
            /* Consume a BLOCK's worth of frames and apply a BLOCK's
             * worth of step. spi_handler scales every profile frame count
             * by DSP4_BLOCK_SIZE, which is right for the ramps that
             * decrement once per SAMPLE. This one decrements once per
             * BLOCK, so taking 1 per block ran it BLOCK times long:
             * measured 2026-08-23 at BLOCK=32, a GainSafe down-ramp took
             * 960 ms against the 30 ms its own cell table specifies, and a
             * GainFast fader move took 85 ms instead of 3 ms. A power of
             * two is exact in binary, so scaling the step loses nothing. */
            r5 = DSP4_BLOCK_SIZE;
            r4 = r4 - r5;
            dm(_fdr_level_frames_C1_FDR_14) = r4;
            f1 = dm(_fdr_level_C1_FDR_14);
            f2 = dm(_fdr_level_step_C1_FDR_14);
            r5 = DSP4_BLOCK_F32;              /* BLOCK_SIZE as float */
            f5 = r5;
            f2 = f2 * f5;
            f1 = f1 + f2;
            dm(_fdr_level_C1_FDR_14) = f1;
            jump (pc, .pramp_C1_FDR_14);
        .lsnap_C1_FDR_14:
            f1 = dm(_fdr_level_target_C1_FDR_14);
            dm(_fdr_level_C1_FDR_14) = f1;
        .pramp_C1_FDR_14:
            /* pan ramp */
            r4 = dm(_fdr_pan_frames_C1_FDR_14);
            r1 = 0;
            comp(r4, r1);
            if le jump (pc, .psnap_C1_FDR_14);
            /* Consume a BLOCK's worth of frames and apply a BLOCK's
             * worth of step. spi_handler scales every profile frame count
             * by DSP4_BLOCK_SIZE, which is right for the ramps that
             * decrement once per SAMPLE. This one decrements once per
             * BLOCK, so taking 1 per block ran it BLOCK times long:
             * measured 2026-08-23 at BLOCK=32, a GainSafe down-ramp took
             * 960 ms against the 30 ms its own cell table specifies, and a
             * GainFast fader move took 85 ms instead of 3 ms. A power of
             * two is exact in binary, so scaling the step loses nothing. */
            r5 = DSP4_BLOCK_SIZE;
            r4 = r4 - r5;
            dm(_fdr_pan_frames_C1_FDR_14) = r4;
            f1 = dm(_fdr_pan_C1_FDR_14);
            f2 = dm(_fdr_pan_step_C1_FDR_14);
            r5 = DSP4_BLOCK_F32;              /* BLOCK_SIZE as float */
            f5 = r5;
            f2 = f2 * f5;
            f1 = f1 + f2;
            dm(_fdr_pan_C1_FDR_14) = f1;
            jump (pc, .cvt_C1_FDR_14);
        .psnap_C1_FDR_14:
            f1 = dm(_fdr_pan_target_C1_FDR_14);
            dm(_fdr_pan_C1_FDR_14) = f1;
        .cvt_C1_FDR_14:
            /* level -> Q4.28 shadow(s) */
            f1 = dm(_fdr_level_C1_FDR_14);
            r2 = 0x4D800000;
            f2 = r2;
            f3 = f1 * f2;
            r2 = fix f3;
            /* CROSSPOINT-COEFFICIENT FOLD (08-25 mandate): mute is a LINEAR
             * gain term, so it belongs in the coefficient at control rate.
             * x*0 is exactly 0 in this format, so the sample path needs no
             * test -- it is one MAC that never reads control state. */
            r3 = dm(_fdr_mute_C1_FDR_14);
            r4 = 0;
            comp(r3, r4);
            if ne r2 = r4;
            dm(_fdr_gq_C1_FDR_14) = r2;
/* L/R pan gains (linear pan law, matches float node).
 *
 * PAN GAIN ONLY -- do NOT fold `comp` in here. The sample path
 * below multiplies the ALREADY-POST-FADER mono by these, so
 * including comp applied the fader twice and the bus feed came
 * out as x * level^2 * (1-pan). It is invisible at unity, which
 * is how it shipped: bench 2026-08-23 measured the main bus
 * 6.02 dB low at level 0.5 and 12.04 dB low at level 0.25, and
 * exact at level 1.0. The float node this was ported from does
 * `f1 = f14 * f7` with f7 = 1 - pan and no comp -- the squaring
 * was introduced by the fixed-point port, not inherited. */
r2 = 0x3F800000;
f2 = r2;
f5 = dm(_fdr_pan_C1_FDR_14);
f6 = f2 - f5;                     /* L gain = 1 - pan */
r2 = 0x4D800000;
f7 = r2;
f6 = f6 * f7;
r2 = fix f6;
dm(_fdr_lq_C1_FDR_14) = r2;
f5 = f5 * f7;
r2 = fix f5;
dm(_fdr_rq_C1_FDR_14) = r2;

        #if DSP4_BLOCK_KERNELS
            /* Per-BLOCK kernel. Same shape that gave GAIN its 4x: the
             * coefficient is hoisted and _mrf_rns28 is inlined with its
             * constants held. Mute is already inside _fdr_gq. */
            r1 = dm(_fdr_gq_C1_FDR_14);
            r7 = 0x08000000;                  /* rounding half */
            r12 = 1;
            r10 = 0x7FFFFFFF;
            l0 = 0;
            l1 = 0;
            l2 = 0;
            l3 = 0;
            i0 = BLK_CHAIN_B;                 /* input  */
            i1 = BLK_CHAIN_A;                 /* mono   */
        #if DSP4_STRIP_FUSED
            /* FUSED (2026-08-28): two samples per iteration, interleaved,
             * second accumulator in MRB -- the same treatment as GAIN.
             * The bigger change here is the LOOP: the unfused body is a
             * manual counter with a branch at the bottom, which a
             * hardware loop replaces outright. The pan legs are already
             * gone (the 08-25 crosspoint fold), so the body really is one
             * MAC per sample. Identical arithmetic, so bit-exact by
             * construction. */
            r14 = DSP4_BLOCK_HALF;
            lcntr = r14; do .fdr_lp_C1_FDR_14 until lce;
                r0 = dm(i0, 1);                 /* xA */
                r3 = dm(i0, 1);                 /* xB */
                mrf = r0 * r1 (ssi);
                mrb = r3 * r1 (ssi);
                mrf = mrf + r7 * r12 (ssi);
                mrb = mrb + r7 * r12 (ssi);
                r8 = mr0f;
                r5 = mr0b;
                r2 = mr1f;
                r4 = mr1b;
                r8 = lshift r8 by -28;
                r5 = lshift r5 by -28;
                r9 = lshift r2 by 4;
                r6 = lshift r4 by 4;
                r0 = r8 or r9;                  /* yA candidate */
                r3 = r5 or r6;                  /* yB candidate */
                r8 = ashift r2 by -28;
                r5 = ashift r4 by -28;
                r9 = ashift r0 by -31;
                r6 = ashift r3 by -31;
                r11 = ashift r2 by -31;
                r13 = ashift r4 by -31;
                r11 = r10 xor r11;
                r13 = r10 xor r13;
                comp(r8, r9);
                if ne r0 = r11;                 /* yA saturated */
                comp(r5, r6);
                if ne r3 = r13;                 /* yB saturated */
                dm(i1, 1) = r0;
        .fdr_lp_C1_FDR_14:
                dm(i1, 1) = r3;
            dm(_tap_post_fader_C1_FDR_14) = r3;   /* linkage scalars */
            dm(_buf_C1_FDR_14) = r3;
            rts;
        #else
            r14 = DSP4_BLOCK_SIZE;
        .fdr_lp_C1_FDR_14:
            r0 = dm(i0, 1);
            mrf = r0 * r1 (ssi);
            mrf = mrf + r7 * r12 (ssi);
            r8 = mr0f;
            r2 = mr1f;
            r8 = lshift r8 by -28;
            r9 = lshift r2 by 4;
            r0 = r8 or r9;
            r8 = ashift r2 by -28;
            r9 = ashift r0 by -31;
            r11 = ashift r2 by -31;
            r11 = r10 xor r11;
            comp(r8, r9);
            if ne r0 = r11;
            dm(i1, 1) = r0;
            r13 = r0;
            r14 = r14 - 1;
            if gt jump (pc, .fdr_lp_C1_FDR_14);
            dm(_tap_post_fader_C1_FDR_14) = r13;  /* linkage scalars */
            dm(_buf_C1_FDR_14) = r13;
            rts;
        #endif
#else
        .apply_C1_FDR_14:
            /* Pure MAC. Mute is already inside _fdr_gq; the pan legs are
             * ROUTING's main-bus crosspoint coefficients. */
            r0 = dm(_buf_C1_DLY_14);
            r1 = dm(_fdr_gq_C1_FDR_14);
            mrf = r0 * r1 (ssi);
            call _mrf_rns28;

            dm(_tap_post_fader_C1_FDR_14) = r0;
            dm(_buf_C1_FDR_14) = r0;

            rts;
#endif
        _C1_FDR_14_process.end:

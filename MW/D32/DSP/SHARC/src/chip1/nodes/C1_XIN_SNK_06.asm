/*----------------------------------------------------------------------
 * Snake Return 6 (INPUT_TDM)
 * Node ID:    C1_XIN_SNK_06
 * Chip:       1
 * Channels:   1
 * SPI Page:   -1
 * SPI Addr:   -1
 * RampProfile: (none)
 *
 * AUTO-GENERATED by tools/dsp/dsp_codegen.py — do not edit directly.
 *----------------------------------------------------------------------*/

/* RampProfile: Instant (default — no ramp) */

/* INPUT_TDM: Read from SPORT5 TDM slot 5 */

#include "blk_pool.h"

.section/dm seg_dmda;
/* Under block kernels this kernel reads the DMA buffer directly,
 * so the slot var is unreferenced -- kept as a scalar purely so
 * block_io.asm's tables still resolve. */
.global _rx_slot_C1_XIN_SNK_06;
.var _rx_slot_C1_XIN_SNK_06;
#if DSP4_BLOCK_KERNELS
.global _buf_C1_XIN_SNK_06;
.var _buf_C1_XIN_SNK_06[32];
#else
.global _buf_C1_XIN_SNK_06;
.var _buf_C1_XIN_SNK_06;
#endif

.section/pm seg_pmco;
.global _C1_XIN_SNK_06_process;
_C1_XIN_SNK_06_process:
#if DSP4_BLOCK_KERNELS
    /* per-BLOCK kernel: one call per block, loop inside */
    l0 = 0;
    l1 = 0;
    /* Read this channel's DMA lane straight into the pool,
     * converting Q1.31 -> Q4.28 on the way. off/stride come from
     * the lane layout, handed to this node by gen_block_io. */
    .extern _rx_active_buf;
    .extern _c1_rx_node_entry;
    .extern _c1_rx_off;
    .extern _c1_rx_stride;
    /* Look the DMA geometry up rather than hardcoding it, so the
     * boot-time input patch still applies. Block rate, not per
     * sample, so it costs nothing measurable. */
    r3 = 40;
    m0 = r3;
    i1 = _c1_rx_node_entry;
    modify(i1, m0);
    r3 = dm(i1, 0);               /* my RX table entry */
    m0 = r3;
    i1 = _c1_rx_off;
    modify(i1, m0);
    r3 = dm(i1, 0);               /* off    */
    i1 = _c1_rx_stride;
    modify(i1, m0);
    r4 = dm(i1, 0);               /* stride */
    r6 = dm(_rx_active_buf);
    r3 = r6 + r3;
    i0 = r3;
    m0 = r4;
    i1 = _buf_C1_XIN_SNK_06;
    r5 = 32;
    lcntr = r5; do .in_lp_C1_XIN_SNK_06 until lce;
        r2 = dm(i0, m0);
        r2 = ashift r2 by -3;
        dm(i1, 1) = r2;
.in_lp_C1_XIN_SNK_06:
        nop;
    rts;
#else
    r0 = dm(_rx_slot_C1_XIN_SNK_06);
    dm(_buf_C1_XIN_SNK_06) = r0;
    rts;
#endif
_C1_XIN_SNK_06_process.end:

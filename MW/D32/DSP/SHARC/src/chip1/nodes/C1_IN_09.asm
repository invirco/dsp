/*----------------------------------------------------------------------
 * Ch 9 Input (INPUT_TDM)
 * Node ID:    C1_IN_09
 * Chip:       1
 * Channels:   1
 * SPI Page:   -1
 * SPI Addr:   -1
 * RampProfile: (none)
 *
 * AUTO-GENERATED by tools/dsp/dsp_codegen.py — do not edit directly.
 *----------------------------------------------------------------------*/

/* RampProfile: Instant (default — no ramp) */

/* INPUT_TDM: Read from SPORT1 TDM slot 0 */

#include "blk_pool.h"

.section/dm seg_dmda;
/* Under block kernels this kernel reads the DMA buffer directly,
 * so the slot var is unreferenced -- kept as a scalar purely so
 * block_io.asm's tables still resolve. The block output lives in
 * the SHARED pool; _buf_ is kept for unconverted consumers and
 * carries the last sample of the block. */
.global _rx_slot_C1_IN_09;
.var _rx_slot_C1_IN_09;
.global _buf_C1_IN_09;
.var _buf_C1_IN_09;

.section/pm seg_pmco;
.global _C1_IN_09_process;
_C1_IN_09_process:
#if DSP4_BLOCK_KERNELS
    /* per-BLOCK kernel: one call per block, loop inside */
    l0 = 0;
    l1 = 0;
    /* Read this channel's DMA lane straight into the pool,
     * converting Q1.31 -> Q4.28 on the way. off/stride come from
     * the lane layout, handed to this node by gen_block_io. */
    .extern _rx_active_buf;
    r6 = dm(_rx_active_buf);
    r3 = 256;
    r3 = r6 + r3;
    i0 = r3;
    r4 = 8;
    m0 = r4;
    i1 = BLK_CHAIN_A;
    r5 = 32;
    lcntr = r5; do .in_lp_C1_IN_09 until lce;
        r2 = dm(i0, m0);
        r2 = ashift r2 by -3;
        dm(i1, 1) = r2;
.in_lp_C1_IN_09:
        nop;
    dm(_buf_C1_IN_09) = r2;   /* linkage scalar */
    rts;
#else
    r0 = dm(_rx_slot_C1_IN_09);
    dm(_buf_C1_IN_09) = r0;
    rts;
#endif
_C1_IN_09_process.end:

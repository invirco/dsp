/*
 * ghost_cells.h — DSP cell definitions for H1S1 MCU firmware
 *
 * AUTO-GENERATED by gen_dsp.py — do not edit directly.
 *
 * The actual array is defined in ghost_cells.c (one translation unit).
 *
 * ramp_up_frames / ramp_down_frames are RAMP-ENGINE FRAMES, and a
 * frame is one audio BLOCK. Generated for DSP4_BLOCK_SIZE = 16
 * (0.3333 ms/frame at 48 kHz). Change the block size in
 * MW/D32/DSP/SHARC/shipping.config and REGENERATE: these counts
 * move with it, and so does the panel MCU copy of this file.
 */
#ifndef GHOST_CELLS_H
#define GHOST_CELLS_H

#include <stdint.h>

typedef struct {
    const char *name;
    uint8_t     chip;
    uint8_t     spi_page;
    uint16_t    addr;
    const char *table;
    uint8_t     ramp_mode;    /* 0=Instant,1=Slew,2=LinearFrames */
    uint16_t    ramp_up_frames;
    uint16_t    ramp_down_frames;
    uint8_t     ramp_scope;   /* 0=Scalar,1=CoeffSetAtomic */
} CellDef;

#define GHOST_CELLS_COUNT 5585

extern const CellDef ghost_cells[5585];

#endif /* GHOST_CELLS_H */

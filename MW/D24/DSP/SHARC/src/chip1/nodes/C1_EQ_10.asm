/*----------------------------------------------------------------------
 * Ch 10 EQ (EQ_BIQUAD)
 * Node ID:    C1_EQ_10
 * Chip:       1
 * Channels:   1
 * SPI Page:   2
 * SPI Addr:   460
 *
 * AUTO-GENERATED by dsp_codegen.py — do not edit directly.
 * Implement DSP logic in the marked section below.
 *----------------------------------------------------------------------*/

/* EQ_BIQUAD: 4-band parametric EQ (biquad cascade) */
/* SPI-controllable: page=2 addr=460 */
/* Consider using ADSP-21564 hardware IIR accelerator for this node */

.section/dm seg_dmda;
.var _eq_coeffs_C1_EQ_10[20];  /* 4 bands × 5 coeffs (b0,b1,b2,a1,a2) */
.var _eq_state_C1_EQ_10[8];    /* 4 bands × 2 state vars (w1,w2) */
.var _buf_C1_EQ_10;

.section/pm seg_pmco;
.global _C1_EQ_10_process;
_C1_EQ_10_process:
    /* Read input */
    r0 = dm(_buf_C1_GAIN_10);

    /* --- Biquad cascade: 4 stages --- */
    /* Direct Form II Transposed:
     *   w0 = x - a1*w1 - a2*w2
     *   y  = b0*w0 + b1*w1 + b2*w2
     *   w2 = w1; w1 = w0
     *
     * TODO: Implement using hardware IIR engine or manual loop.
     * For HW IIR: configure IIR accelerator registers, load coeffs,
     * trigger, read result.
     */

    /* Placeholder: pass-through */
    dm(_buf_C1_EQ_10) = r0;
    rts;
_C1_EQ_10_process.end:

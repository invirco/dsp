/*----------------------------------------------------------------------
 * Master EQ R (EQ_MASTER)
 * Node ID:    C2_EQ_MAIN_R
 * Chip:       2
 * Channels:   1
 * SPI Page:   6
 * SPI Addr:   140
 *
 * AUTO-GENERATED by dsp_codegen.py — do not edit directly.
 * Implement DSP logic in the marked section below.
 *----------------------------------------------------------------------*/

/* EQ_BIQUAD: 4-band parametric EQ (biquad cascade) */
/* SPI-controllable: page=6 addr=140 */
/* Consider using ADSP-21564 hardware IIR accelerator for this node */

.section/dm seg_dmda;
.var _eq_coeffs_C2_EQ_MAIN_R[20];  /* 4 bands × 5 coeffs (b0,b1,b2,a1,a2) */
.var _eq_state_C2_EQ_MAIN_R[8];    /* 4 bands × 2 state vars (w1,w2) */
.var _buf_C2_EQ_MAIN_R;

.section/pm seg_pmco;
.global _C2_EQ_MAIN_R_process;
_C2_EQ_MAIN_R_process:
    /* Read input */
    r0 = dm(_buf_C2_REVERB);

    /* --- Biquad cascade: 4 stages --- */
    /* Direct Form II Transposed:
     *   w0 = x - a1*w1 - a2*w2
     *   y  = b0*w0 + b1*w1 + b2*w2
     *   w2 = w1; w1 = w0
     *
     * TODO: Implement using hardware IIR engine or manual loop.
     * For HW IIR: configure IIR accelerator registers, load coeffs,
     * trigger, read result.
     */

    /* Placeholder: pass-through */
    dm(_buf_C2_EQ_MAIN_R) = r0;
    rts;
_C2_EQ_MAIN_R_process.end:

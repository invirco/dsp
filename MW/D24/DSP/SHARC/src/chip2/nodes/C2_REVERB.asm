/*----------------------------------------------------------------------
 * Main Reverb (REVERB)
 * Node ID:    C2_REVERB
 * Chip:       2
 * Channels:   2
 * SPI Page:   5
 * SPI Addr:   100
 *
 * AUTO-GENERATED by dsp_codegen.py — do not edit directly.
 * Implement DSP logic in the marked section below.
 *----------------------------------------------------------------------*/

/* REVERB: Stereo Freeverb */
/* SPI-controllable: page=5 addr=100 */
/* room_size=0.7 damping=0.5 */
/* wet=0.3 dry=0.7 width=1.0 */

.section/dm seg_dmda;
.var _reverb_room_size_C2_REVERB;
.var _reverb_damping_C2_REVERB;
.var _reverb_wet_C2_REVERB;
.var _reverb_dry_C2_REVERB;
.var _reverb_width_C2_REVERB;

/* Freeverb uses 8 parallel comb filters + 4 series allpass filters per channel */
/* Delay line storage in L2 SRAM — ~50KB per stereo reverb instance */
.var _reverb_comb_buf_L_C2_REVERB[8*1557];   /* 8 comb filters, ~1500 samples avg */
.var _reverb_comb_buf_R_C2_REVERB[8*1557];
.var _reverb_allpass_buf_L_C2_REVERB[4*556];  /* 4 allpass filters */
.var _reverb_allpass_buf_R_C2_REVERB[4*556];

.var _buf_L_C2_REVERB;
.var _buf_R_C2_REVERB;

.section/pm seg_pmco;
.global _C2_REVERB_process;
_C2_REVERB_process:
    /* --- Freeverb algorithm --- */
    /* 1. Read stereo input from mix bus */
    /* 2. Process 8 parallel comb filters (LPF in feedback path) per channel */
    /* 3. Process 4 series allpass filters per channel */
    /* 4. Mix wet/dry */
    /* 5. Apply stereo width */

    /* TODO: Port from ADI sam-audio-starter effect_stereo_reverb.c */
    /* Optimise inner loops: comb filter + allpass use circular buffers */
    /* Use SHARC+ circular buffer hardware (I/L/B/M registers) */

    /* Placeholder: pass-through */
    rts;
_C2_REVERB_process.end:
